{
  "name": "candy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "discover": "node --env-file-if-exists=.env.local --import tsx scripts/discover.mts",
    "crawl": "node --env-file-if-exists=.env.local --import tsx scripts/crawl.mts",
    "stats": "node --env-file-if-exists=.env.local --import tsx scripts/stats.mts"
  },
  "dependencies": {
    "ioredis": "^6.0.0",
    "next": "^15",
    "react": "^19",
    "react-dom": "^19",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tsx": "^4",
    "typescript": "^5"
  }
}
